// Capture-only transport adapter: original components request the same Inter font.
// @font-face files are served locally by round3.html; no motion code is changed.
export const loadFont = () => ({fontFamily:'Inter'});
